  <% if obj.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(obj.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
