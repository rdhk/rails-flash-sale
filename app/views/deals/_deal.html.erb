<div class = "col-md-5">
  <div class="thumbnail">
    <div id="carousel-example-generic" class="carousel slide" data-ride="carousel">
      <div class="carousel-inner" role="listbox">
        <% deal.images.each_with_index do |img, index| %>
        <% #FIXME_AB: don't use inline styles - done -%>
        <div class='fixed-width col-md-offset-1 item <%= (index == 0) ? "active" : "" %>'>
          <%= image_tag(img.file.url(:medium), class: "img-responsive") %>
        </div>
        <% end %>
      </div>
    </div>
    <div class="caption-full">
      <h4 class="pull-right"><s><%= number_to_currency deal.price, unit: "Rs" %></s> <%= number_to_currency deal.discounted_price, unit: "Rs" %></h4>
      <h4><%= link_to deal.title, deal_path(deal) %>
      </h4>
    </div>
    <div class="ratings">
    <%= expired?(deal) %>
    </div>
  </div>
</div>
